<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.sqlite_bd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegisterUserActivity" />
        <activity android:name=".activities.ConsultaUserActivity" />
        <activity android:name=".activities.CSpinnerActivity" />
        <activity android:name=".activities.CUserListViewActivity" />
        <activity android:name=".activities.DetalleListVActivity" />
        <activity android:name=".activities.RegisterMascoActivity" />
        <activity android:name=".activities.ConsultaMActivity" />
        <activity android:name=".activities.DetalleMascotasActivity"></activity>
    </application>

</manifest>